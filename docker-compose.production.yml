version: '3'

volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:13
    env_file: ./kittygram_final/.env
    volumes:
      - pg_data:/var/lib/postgresql/data
  backend:
    image: pullveryzator/kittygram_backend
    env_file: ./kittygram_final/.env
    volumes:
      - static:/static
      - media:/media
  frontend:
    env_file: ./kittygram_final/.env
    image: pullveryzator/kittygram_frontend
    command: cp -r /app/build/. /static/
    volumes:
      - static:/static
  gateway:
    image: pullveryzator/kittygram_gateway
    env_file: ./kittygram_final/.env
    ports:
      - 9000:80
    volumes:
      - static:/static
      - media:/media
